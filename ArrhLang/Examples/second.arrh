def CALC 0
def SUM 1
[
  CALC => (a, b) {
    $a + $b
  }
  SUM => 10
  MAIN => (a,b){
    [SUM] = [CALC]($a,$b)
    out([SUM])
    out('¨')
    if [SUM] > 2
      out('Awesome sauce!¨')

    out('The end')
  }
]